# Improves compatibility with Expo + Metro in a pnpm monorepo
node-linker=hoisted
shamefully-hoist=true
# Firebase / Expo peer ranges can disagree on async-storage major; install still works
strict-peer-dependencies=false
