import React from "react";
import UserProfile from "../screens/bottomTabs/(user)/profile";

/** Profile tab — same user profile shell for every role (no admin app). */
export default function ProfileTabScreen() {
  return <UserProfile />;
}
