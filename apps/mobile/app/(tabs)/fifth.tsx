import React from "react";
import UserProfile from "../screens/bottomTabs/(user)/profile";

/** Profile tab — same user profile shell for every role. */
export default function ProfileTabScreen() {
  return <UserProfile />;
}
