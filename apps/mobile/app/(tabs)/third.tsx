import React from "react";
import UnifiedPeopleScreen from "../screens/unified/UnifiedPeopleScreen";

/** People tab — workers and contractors for every signed-in user. */
export default function PeopleTabScreen() {
  return <UnifiedPeopleScreen />;
}
