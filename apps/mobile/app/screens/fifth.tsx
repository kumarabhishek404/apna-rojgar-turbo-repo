import React from "react";
import UserProfile from "./bottomTabs/(user)/profile";

/** Legacy route — always the user profile (admin shell removed). */
export default function FifthScreen() {
  return <UserProfile />;
}
