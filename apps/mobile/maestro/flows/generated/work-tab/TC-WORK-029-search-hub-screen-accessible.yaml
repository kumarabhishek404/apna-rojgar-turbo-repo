appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "availability", "all", "automated"]
---
# TC-WORK-029: Search hub screen accessible
# Module: Tab: Work | Screen: Search Hub | Role: All
# Type: Availability | Priority: P2
# Route: /screens/bottomTabs/(user)/search/index
# Prerequisites: Any test account · Navigate to search hub
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open search index if linked.

# Assertions
# Expected: Search hub with services/workers options.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
