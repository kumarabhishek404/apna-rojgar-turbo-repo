appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["service-listing-detail", "functionality", "all", "automated"]
---
# TC-SVC-039: Favourites list shows liked services only
# Module: Service Listing & Detail | Screen: Service List | Role: All
# Type: Functionality | Priority: P1
# Route: /screens/favourite
# Prerequisites: Any test account · Liked services
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open favourites.
# Compare to liked items.

# Assertions
# Expected: Only liked services listed.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
