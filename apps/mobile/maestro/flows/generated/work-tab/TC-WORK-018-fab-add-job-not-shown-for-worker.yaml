appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "availability", "worker", "automated"]
---
# TC-WORK-018: FAB add job NOT shown for Worker
# Module: Tab: Work | Screen: FAB Add Job | Role: Worker
# Type: Availability | Priority: P1
# Route: UnifiedTabFab
# Prerequisites: Worker account · Worker on work tab
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe work tab as Worker.

# Assertions
# Expected: No add job FAB or different FAB action.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
