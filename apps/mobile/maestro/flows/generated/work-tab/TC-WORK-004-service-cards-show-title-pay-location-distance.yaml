appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "ui-design", "worker", "automated"]
---
# TC-WORK-004: Service cards show title pay location distance
# Module: Tab: Work | Screen: Service List | Role: Worker
# Type: UI Design | Priority: P1
# Route: allServices.tsx
# Prerequisites: Worker account · Jobs on server
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe service cards in list.

# Assertions
# Expected: Key job info visible on each card.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
