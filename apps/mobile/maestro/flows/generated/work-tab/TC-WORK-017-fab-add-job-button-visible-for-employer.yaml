appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "availability", "employer", "automated"]
---
# TC-WORK-017: FAB add job button visible for Employer
# Module: Tab: Work | Screen: FAB Add Job | Role: Employer
# Type: Availability | Priority: P0
# Route: UnifiedTabFab
# Prerequisites: Employer account · Employer on work tab
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe work tab bottom-right.

# Assertions
# Expected: Floating action button for add job visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
