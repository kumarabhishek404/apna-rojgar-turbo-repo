appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "functionality", "worker", "automated"]
---
# TC-WORK-028: Clear filters resets list
# Module: Tab: Work | Screen: Filter Services | Role: Worker
# Type: Functionality | Priority: P2
# Route: filterServices.tsx
# Prerequisites: Worker account · Filters applied
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Apply filters.
# Clear/reset.
# Observe list.

# Assertions
# Expected: Full unfiltered list restored.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
