appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "functionality", "employer", "automated"]
---
# TC-WORK-006: Tap worker card opens user profile
# Module: Tab: Work | Screen: Worker List | Role: Employer
# Type: Functionality | Priority: P0
# Route: allWorkers.tsx
# Prerequisites: Employer account · Workers on server
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "worker card"

# Assertions
# Expected: Navigates to /screens/users/[id].

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
