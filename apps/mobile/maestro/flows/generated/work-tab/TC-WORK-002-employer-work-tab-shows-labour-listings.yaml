appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "availability", "employer", "automated"]
---
# TC-WORK-002: Employer work tab shows labour listings
# Module: Tab: Work | Screen: Work Tab | Role: Employer
# Type: Availability | Priority: P0
# Route: /(tabs)/second
# Prerequisites: Employer account · Employer logged in
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Work tab as Employer.

# Assertions
# Expected: AllWorkers list with worker cards visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
