appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["reviews-ratings", "availability", "all", "automated"]
---
# TC-REV-010: Rating component on user profile
# Module: Reviews & Ratings | Screen: Rating Display | Role: All
# Type: Availability | Priority: P1
# Route: RatingAndReviews
# Prerequisites: Any test account · User profile with rating
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open user profile.
# Observe rating.

# Assertions
# Expected: Average stars and count shown.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
