appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["reviews-ratings", "ui-design", "all", "automated"]
---
# TC-REV-014: Star rating tap targets adequate size
# Module: Reviews & Ratings | Screen: Add Review | Role: All
# Type: UI Design | Priority: P1
# Route: /screens/reviews/addReview/[id]
# Prerequisites: Any test account · On add review
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "each star"

# Assertions
# Expected: Stars select reliably on mobile.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
