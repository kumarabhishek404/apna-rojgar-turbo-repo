appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["reviews-ratings", "availability", "all", "automated"]
---
# TC-REV-004: Add review form loads
# Module: Reviews & Ratings | Screen: Add Review | Role: All
# Type: Availability | Priority: P0
# Route: /screens/reviews/addReview/[id]
# Prerequisites: Any test account · Eligible to review user
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open add review for a user.

# Assertions
# Expected: Star selector, reasons, feedback text visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
