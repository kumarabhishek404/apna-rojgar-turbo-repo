appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["service-listing-detail", "functionality", "employer", "automated"]
---
# TC-SVC-017: Employer view selected workers list
# Module: Service Listing & Detail | Screen: Selections Tab | Role: Employer
# Type: Functionality | Priority: P0
# Route: selectedApplicants.tsx
# Prerequisites: Employer account · Selected workers
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Selections tab.

# Assertions
# Expected: Selected workers listed.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
