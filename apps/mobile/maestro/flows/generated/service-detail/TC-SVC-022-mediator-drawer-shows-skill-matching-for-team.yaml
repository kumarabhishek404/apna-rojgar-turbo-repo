appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["service-listing-detail", "ui-design", "mediator", "automated"]
---
# TC-SVC-022: Mediator drawer shows skill matching for team
# Module: Service Listing & Detail | Screen: Apply Mediator Drawer | Role: Mediator
# Type: UI Design | Priority: P1
# Route: ApplyAsMediatorDrawer
# Prerequisites: Mediator account · Drawer open
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open apply drawer.
# Observe member skill match.

# Assertions
# Expected: Skill match indicators visible per member.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
