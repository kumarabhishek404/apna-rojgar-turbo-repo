appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["team-mediator", "functionality", "mediator", "automated"]
---
# TC-TEAM-009: Select subset of team members for job apply
# Module: Team / Mediator | Screen: Apply as Mediator | Role: Mediator
# Type: Functionality | Priority: P0
# Route: ApplyAsMediatorDrawer
# Prerequisites: Mediator account · Mediator with team on job detail
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open apply drawer.
# Select some members.
# Apply.

# Assertions
# Expected: Application includes only selected members.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
