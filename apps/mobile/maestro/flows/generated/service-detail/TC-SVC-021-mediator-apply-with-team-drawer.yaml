appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["service-listing-detail", "functionality", "mediator", "automated"]
---
# TC-SVC-021: Mediator apply with team drawer
# Module: Service Listing & Detail | Screen: Service Detail | Role: Mediator
# Type: Functionality | Priority: P0
# Route: /screens/service/[id]
# Prerequisites: Mediator account · Mediator with team
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "Apply as Team"
# Select team members.
# Submit.

# Assertions
# Expected: Team application submitted.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
