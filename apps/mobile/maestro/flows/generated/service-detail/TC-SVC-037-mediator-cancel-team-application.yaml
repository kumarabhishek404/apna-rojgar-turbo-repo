appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["service-listing-detail", "functionality", "mediator", "automated"]
---
# TC-SVC-037: Mediator cancel team application
# Module: Service Listing & Detail | Screen: Service Detail | Role: Mediator
# Type: Functionality | Priority: P1
# Route: /screens/service/[id]
# Prerequisites: Mediator account · Already applied as team
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Cancel team apply if available.

# Assertions
# Expected: Application cancelled.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
