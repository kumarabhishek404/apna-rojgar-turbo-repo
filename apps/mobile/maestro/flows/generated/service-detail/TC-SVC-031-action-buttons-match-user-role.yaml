appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["service-listing-detail", "availability", "all", "automated"]
---
# TC-SVC-031: Action buttons match user role
# Module: Service Listing & Detail | Screen: Action Buttons | Role: All
# Type: Availability | Priority: P1
# Route: actionButtons.tsx
# Prerequisites: Any test account · On service detail per role
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open same job as Worker, Employer, Mediator.
# Compare buttons.

# Assertions
# Expected: Role-appropriate actions only shown.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
