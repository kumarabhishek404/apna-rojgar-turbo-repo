appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["team-mediator", "availability", "mediator", "automated"]
---
# TC-TEAM-028: Apply as team button visible for mediator
# Module: Team / Mediator | Screen: Apply as Mediator | Role: Mediator
# Type: Availability | Priority: P0
# Route: ApplyAsMediatorDrawer
# Prerequisites: Mediator account · Job detail as mediator
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open job as Mediator.

# Assertions
# Expected: Team apply action visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
