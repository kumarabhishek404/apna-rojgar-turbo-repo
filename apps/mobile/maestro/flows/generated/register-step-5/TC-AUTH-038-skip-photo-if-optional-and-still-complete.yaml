appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["authentication", "functionality", "guest", "automated"]
---
# TC-AUTH-038: Skip photo if optional and still complete
# Module: Authentication | Screen: Register Step 5 | Role: Guest
# Type: Functionality | Priority: P2
# Route: /screens/auth/register/fifth
# Prerequisites: Logged out · On step 5
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Attempt finish without photo if skip available.

# Assertions
# Expected: Behavior matches product rules (required vs optional).

# Cleanup
