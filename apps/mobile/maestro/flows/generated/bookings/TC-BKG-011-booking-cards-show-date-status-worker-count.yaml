appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["bookings-invitations", "ui-design", "all", "automated"]
---
# TC-BKG-011: Booking cards show date status worker count
# Module: Bookings & Invitations | Screen: Bookings List | Role: All
# Type: UI Design | Priority: P1
# Route: ListingVerticalBookings
# Prerequisites: Any test account · Bookings in list
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe booking cards.

# Assertions
# Expected: Key booking info on card.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
