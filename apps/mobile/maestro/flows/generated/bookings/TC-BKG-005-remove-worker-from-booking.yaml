appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["bookings-invitations", "functionality", "employer", "automated"]
---
# TC-BKG-005: Remove worker from booking
# Module: Bookings & Invitations | Screen: Booking Actions | Role: Employer
# Type: Functionality | Priority: P1
# Route: actionButtons.tsx
# Prerequisites: Employer account · Booking with workers
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Remove a booked worker.

# Assertions
# Expected: Worker removed from booking.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
