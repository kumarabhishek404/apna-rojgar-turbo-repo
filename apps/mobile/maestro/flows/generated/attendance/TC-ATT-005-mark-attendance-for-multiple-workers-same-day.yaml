appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["attendance", "functionality", "employer", "automated"]
---
# TC-ATT-005: Mark attendance for multiple workers same day
# Module: Attendance | Screen: Add Attendance | Role: Employer
# Type: Functionality | Priority: P1
# Route: /screens/bookings/addAttendance
# Prerequisites: Employer account · Multiple workers in booking
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Set attendance for each worker.
# Submit all.

# Assertions
# Expected: All records saved.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
