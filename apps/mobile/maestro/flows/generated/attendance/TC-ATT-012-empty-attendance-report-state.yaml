appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["attendance", "ui-design", "employer", "automated"]
---
# TC-ATT-012: Empty attendance report state
# Module: Attendance | Screen: Show Attendance | Role: Employer
# Type: UI Design | Priority: P2
# Route: /screens/bookings/showAttendance
# Prerequisites: Employer account · No attendance data
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open report with no records.

# Assertions
# Expected: Empty state message.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
