appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["attendance", "availability", "worker", "automated"]
---
# TC-ATT-013: Worker cannot access add attendance
# Module: Attendance | Screen: Add Attendance | Role: Worker
# Type: Availability | Priority: P2
# Route: /screens/bookings/addAttendance
# Prerequisites: Worker account · Worker logged in
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Attempt add attendance as Worker.

# Assertions
# Expected: Not accessible or appropriate restriction.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
