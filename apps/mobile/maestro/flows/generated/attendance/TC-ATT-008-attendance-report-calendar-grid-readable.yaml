appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["attendance", "ui-design", "employer", "automated"]
---
# TC-ATT-008: Attendance report calendar/grid readable
# Module: Attendance | Screen: Show Attendance | Role: Employer
# Type: UI Design | Priority: P1
# Route: /screens/bookings/showAttendance
# Prerequisites: Employer account · Multi-day report
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Scroll full report.

# Assertions
# Expected: Dates and workers aligned.
# Expected: readable on mobile.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
