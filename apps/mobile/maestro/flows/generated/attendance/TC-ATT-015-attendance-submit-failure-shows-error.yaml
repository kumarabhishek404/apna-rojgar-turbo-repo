appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["attendance", "functionality", "employer", "automated"]
---
# TC-ATT-015: Attendance submit failure shows error
# Module: Attendance | Screen: Add Attendance | Role: Employer
# Type: Functionality | Priority: P2
# Route: /screens/bookings/addAttendance
# Prerequisites: Employer account · Network error on submit
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Simulate API failure.
# Submit.

# Assertions
# Expected: Error toast.
# Expected: data not lost locally if retry supported.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
