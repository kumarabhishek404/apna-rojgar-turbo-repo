appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["attendance", "functionality", "employer", "automated"]
---
# TC-ATT-011: Validation when no attendance marked
# Module: Attendance | Screen: Add Attendance | Role: Employer
# Type: Functionality | Priority: P2
# Route: /screens/bookings/addAttendance
# Prerequisites: Employer account · Submit without selection
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Submit empty attendance.

# Assertions
# Expected: Validation error or no-op with message.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
