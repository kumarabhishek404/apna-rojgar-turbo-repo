appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["attendance", "availability", "employer", "automated"]
---
# TC-ATT-006: Attendance report screen loads
# Module: Attendance | Screen: Show Attendance | Role: Employer
# Type: Availability | Priority: P0
# Route: /screens/bookings/showAttendance
# Prerequisites: Employer account · Attendance data exists
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open show attendance from booking.

# Assertions
# Expected: Report with dates and worker attendance visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
