appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "ui-design", "employer", "automated"]
---
# TC-GLB-016: Employer tab labels show correct role-specific names
# Module: Global & App Shell | Screen: Bottom Tabs | Role: Employer
# Type: UI Design | Priority: P1
# Route: /(tabs)/_layout.tsx
# Prerequisites: Employer account · Logged in as Employer
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Login as Employer.
# Observe tab bar.

# Assertions
# Expected: Employer-specific tab labels displayed (e.g. Work shows labour listings context).

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
