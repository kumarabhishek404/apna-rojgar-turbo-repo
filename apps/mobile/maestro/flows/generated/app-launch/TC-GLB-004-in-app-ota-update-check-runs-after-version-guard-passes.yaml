appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "functionality", "all", "manual-review"]
---
# TC-GLB-004: In-app OTA update check runs after version guard passes
# Module: Global & App Shell | Screen: OTA Update | Role: All
# Type: Functionality | Priority: P2
# Route: InAppUpdatesChecker
# Prerequisites: Any test account · OTA update available via Expo
# STATUS: MANUAL REVIEW — contains steps not fully automatable
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Launch app with supported version.
# Wait for OTA checker.

# Assertions
# Expected: OTA prompt shown when update available.
- assertNotVisible: "Something went wrong"

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
