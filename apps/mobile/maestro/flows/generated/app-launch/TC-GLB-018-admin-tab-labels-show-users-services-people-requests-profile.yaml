appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "ui-design", "admin", "automated"]
---
# TC-GLB-018: Admin tab labels show Users, Services, People, Requests, Profile
# Module: Global & App Shell | Screen: Bottom Tabs | Role: Admin
# Type: UI Design | Priority: P1
# Route: /(tabs)/_layout.tsx
# Prerequisites: Admin account · Logged in as Admin
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Login as Admin.
# Observe tab bar labels.

# Assertions
# Expected: Admin tab labels differ from user roles appropriately.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
