appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "functionality", "guest", "automated"]
---
# TC-GLB-008: Incomplete profile redirects to login or register
# Module: Global & App Shell | Screen: Auth Guard | Role: Guest
# Type: Functionality | Priority: P0
# Route: /(tabs)/_layout.tsx
# Prerequisites: Logged out · Incomplete profile or
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Clear token or use account missing name/address/age/gender.
# Attempt to open tabs.

# Assertions
# Expected: User redirected to /screens/auth/login or register step 2.

# Cleanup
