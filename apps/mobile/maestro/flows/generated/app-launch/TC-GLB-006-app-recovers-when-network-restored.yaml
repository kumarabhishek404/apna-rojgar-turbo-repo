appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "functionality", "all", "automated"]
---
# TC-GLB-006: App recovers when network restored
# Module: Global & App Shell | Screen: No Internet | Role: All
# Type: Functionality | Priority: P1
# Route: /screens/placeholders/noInternet
# Prerequisites: Any test account · App showing offline state
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Re-enable network.
- scroll

# Assertions
# Expected: Content loads.
# Expected: offline placeholder dismissed.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
