appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "availability", "all", "automated"]
---
# TC-GLB-022: Error boundary prevents full app white-screen on component error
# Module: Global & App Shell | Screen: Error Boundary | Role: All
# Type: Availability | Priority: P2
# Route: app/_layout.tsx
# Prerequisites: Any test account · None
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Navigate through major screens.
# Verify no unhandled crash loops.

# Assertions
# Expected: Isolated errors show fallback UI.
# Expected: app remains usable.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
