appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "functionality", "all", "automated"]
---
# TC-GLB-014: Infinite scroll loads next page
# Module: Global & App Shell | Screen: Pagination | Role: All
# Type: Functionality | Priority: P1
# Route: List screens
# Prerequisites: Any test account · List with more than one page of data
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Scroll to bottom of list.
# Wait for loader.
# Continue scrolling.

# Assertions
# Expected: Additional items append.
# Expected: loader disappears when no more data.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
