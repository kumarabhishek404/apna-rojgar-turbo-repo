appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "functionality", "all", "manual-review"]
---
# TC-GLB-003: Update button opens Play Store or update flow
# Module: Global & App Shell | Screen: Force Update | Role: All
# Type: Functionality | Priority: P1
# Route: ForceUpdateSection
# Prerequisites: Any test account · Force update screen visible
# STATUS: MANUAL REVIEW — contains steps not fully automatable
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "On force update screen tap Update/Go to Store"
# Observe navigation.

# Assertions
# Expected: User is directed to app update (Play Store or OTA).

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
