appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "ui-design", "all", "automated"]
---
# TC-GLB-012: Success and error toasts display correctly
# Module: Global & App Shell | Screen: Toast | Role: All
# Type: UI Design | Priority: P2
# Route: toast.tsx
# Prerequisites: Any test account · Trigger success and error actions
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Perform action with success response.
# Perform action with error.
# Observe toasts.

# Assertions
# Expected: Green success and red error toasts appear briefly and dismiss.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
