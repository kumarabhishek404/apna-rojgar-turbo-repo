appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "availability", "all", "automated"]
---
# TC-GLB-010: Global bottom drawer opens and closes
# Module: Global & App Shell | Screen: Global Bottom Drawer | Role: All
# Type: Availability | Priority: P1
# Route: GlobalBottomDrawer
# Prerequisites: Any test account · Screen with filter/address drawer trigger
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open filter or address drawer from listing screen.
- tapOn: "Swipe/tap to close"

# Assertions
# Expected: Drawer slides up with content.
# Expected: closes without UI glitch.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
