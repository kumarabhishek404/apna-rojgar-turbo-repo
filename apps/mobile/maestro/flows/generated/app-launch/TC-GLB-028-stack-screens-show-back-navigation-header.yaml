appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["global-app-shell", "ui-design", "all", "automated"]
---
# TC-GLB-028: Stack screens show back navigation header
# Module: Global & App Shell | Screen: Stack Navigation | Role: All
# Type: UI Design | Priority: P1
# Route: app/_layout.tsx
# Prerequisites: Any test account · Logged in
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open any /screens/* route from tabs.
# Observe header.

# Assertions
# Expected: Back button/header visible.
# Expected: returns to previous screen.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
