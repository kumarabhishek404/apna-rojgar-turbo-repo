appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["admin-panel", "functionality", "worker", "automated"]
---
# TC-ADM-026: Non-admin allFeedback access restricted
# Module: Admin Panel | Screen: All Feedback | Role: Worker
# Type: Functionality | Priority: P2
# Route: /screens/allFeedback
# Prerequisites: Worker account · Worker navigates to route
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Attempt /screens/allFeedback as Worker.

# Assertions
# Expected: Blocked or empty.
# Expected: admin-only data.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
