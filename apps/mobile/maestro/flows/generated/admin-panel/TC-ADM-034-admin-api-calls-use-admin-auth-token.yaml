appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["admin-panel", "functionality", "admin", "automated"]
---
# TC-ADM-034: Admin API calls use admin auth token
# Module: Admin Panel | Screen: Admin API | Role: Admin
# Type: Functionality | Priority: P1
# Route: admin.tsx
# Prerequisites: Admin account · Admin session
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Perform suspend action.
# Verify API success.

# Assertions
# Expected: Admin endpoints respond 200 with valid admin token.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
