appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "availability", "admin", "automated"]
---
# TC-ACT-004: Admin activity tab shows requests queue
# Module: Tab: Activity | Screen: Admin Requests Tab | Role: Admin
# Type: Availability | Priority: P0
# Route: /(tabs)/fourth
# Prerequisites: Admin account · Admin logged in
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open tab 4 as Admin.

# Assertions
# Expected: AdminRequests component visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
