appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-work", "ui-design", "admin", "automated"]
---
# TC-WORK-033: Admin service list shows employer and status
# Module: Tab: Work | Screen: Admin Services Tab | Role: Admin
# Type: UI Design | Priority: P1
# Route: AdminServices
# Prerequisites: Admin account · Admin services tab
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe admin service cards.

# Assertions
# Expected: Employer name and job status visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
