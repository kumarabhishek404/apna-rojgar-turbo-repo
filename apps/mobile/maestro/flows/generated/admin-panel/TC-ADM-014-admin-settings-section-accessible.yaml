appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["admin-panel", "availability", "admin", "automated"]
---
# TC-ADM-014: Admin settings section accessible
# Module: Admin Panel | Screen: Admin Settings | Role: Admin
# Type: Availability | Priority: P2
# Route: AdminSettings
# Prerequisites: Admin account · Admin profile settings
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open admin settings.

# Assertions
# Expected: Admin settings placeholders/options visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
