appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["notifications-push", "functionality", "all", "automated"]
---
# TC-NOT-005: Push token registered after login
# Module: Notifications & Push | Screen: Push Registration | Role: All
# Type: Functionality | Priority: P0
# Route: usePushNotification.ts
# Prerequisites: Any test account · Fresh login; notification permission
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Login.
# Grant notification permission.
# Verify token on server.

# Assertions
# Expected: Device token registered via NOTIFICATION.registerDevice.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
