appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["notifications-push", "ui-design", "all", "automated"]
---
# TC-NOT-010: Notification toast UI styled correctly
# Module: Notifications & Push | Screen: Notification Toast | Role: All
# Type: UI Design | Priority: P2
# Route: NotificationToast
# Prerequisites: Any test account · In-app notification trigger
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Trigger in-app notification toast.

# Assertions
# Expected: Toast readable and dismissible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
