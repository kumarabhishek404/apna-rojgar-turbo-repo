appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["notifications-push", "functionality", "all", "automated"]
---
# TC-NOT-016: Local notification triggered for unread while app open
# Module: Notifications & Push | Screen: Local Notifications | Role: All
# Type: Functionality | Priority: P2
# Route: useInAppNotifications.tsx
# Prerequisites: Any test account · Unread on server while app open
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Receive unread while in app.

# Assertions
# Expected: Local notification or banner shown.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
