appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["notifications-push", "functionality", "admin", "automated"]
---
# TC-NOT-021: Admin local notification trigger for testing
# Module: Notifications & Push | Screen: Admin Local Notification | Role: Admin
# Type: Functionality | Priority: P2
# Route: triggerNotificationAdmin.tsx
# Prerequisites: Admin account · Admin dev trigger if available
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Trigger admin test notification if available.

# Assertions
# Expected: Local notification appears.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
