appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["deep-links-sharing", "functionality", "all", "automated"]
---
# TC-DLNK-002: Universal link https://apnarojgarindia.com/job/id opens app
# Module: Deep Links & Sharing | Screen: Universal Link | Role: All
# Type: Functionality | Priority: P0
# Route: /job/[id]
# Prerequisites: Any test account · Universal links configured
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "universal link on Android"
# Observe app opens to job detail.

# Assertions
# Expected: App opens service detail via job bridge.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
