appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["deep-links-sharing", "ui-design", "all", "automated"]
---
# TC-DLNK-011: Human-readable job ID shown for sharing
# Module: Deep Links & Sharing | Screen: Job ID Display | Role: All
# Type: UI Design | Priority: P2
# Route: serviceJobId.ts
# Prerequisites: Any test account · On service detail
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe job ID on detail or share text.

# Assertions
# Expected: Readable job ID included in share content.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
