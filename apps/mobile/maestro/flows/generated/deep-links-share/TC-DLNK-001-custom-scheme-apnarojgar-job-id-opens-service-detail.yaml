appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["deep-links-sharing", "functionality", "all", "automated"]
---
# TC-DLNK-001: Custom scheme apnarojgar://job/id opens service detail
# Module: Deep Links & Sharing | Screen: Job Deep Link Bridge | Role: All
# Type: Functionality | Priority: P0
# Route: /job/[id]
# Prerequisites: Any test account · Valid job ID; app installed
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open apnarojgar://job/<valid-id> via adb or link.
# Observe navigation.

# Assertions
# Expected: Redirects to /screens/service/[id].

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
