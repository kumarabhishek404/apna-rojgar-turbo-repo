appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["deep-links-sharing", "availability", "all", "manual-review"]
---
# TC-DLNK-006: Share app screen loads with options
# Module: Deep Links & Sharing | Screen: Share App | Role: All
# Type: Availability | Priority: P1
# Route: /screens/shareApp
# Prerequisites: Any test account · From profile menu
# STATUS: MANUAL REVIEW — contains steps not fully automatable
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open /screens/shareApp.

# Assertions
# Expected: WhatsApp, SMS, LinkedIn, Copy link options visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
