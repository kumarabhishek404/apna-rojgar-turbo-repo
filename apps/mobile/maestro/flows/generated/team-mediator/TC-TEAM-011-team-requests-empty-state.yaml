appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["team-mediator", "ui-design", "all", "automated"]
---
# TC-TEAM-011: Team requests empty state
# Module: Team / Mediator | Screen: Team Requests | Role: All
# Type: UI Design | Priority: P2
# Route: /screens/teamRequests
# Prerequisites: Any test account · No requests
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open requests with none.

# Assertions
# Expected: Empty state on both tabs.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
