appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["team-mediator", "availability", "worker", "automated"]
---
# TC-TEAM-027: Worker sent tab empty or hidden appropriately
# Module: Team / Mediator | Screen: Team Requests | Role: Worker
# Type: Availability | Priority: P1
# Route: /screens/teamRequests
# Prerequisites: Worker account · Worker not on any team
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Sent tab as worker.

# Assertions
# Expected: Empty or N/A state.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
