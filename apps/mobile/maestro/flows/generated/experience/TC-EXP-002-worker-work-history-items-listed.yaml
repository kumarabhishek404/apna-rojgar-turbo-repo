appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["experience-history", "functionality", "worker", "automated"]
---
# TC-EXP-002: Worker work history items listed
# Module: Experience History | Screen: Experience | Role: Worker
# Type: Functionality | Priority: P1
# Route: /screens/experience
# Prerequisites: Worker account · Worker with completed jobs
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open experience as Worker.

# Assertions
# Expected: Past work/bookings shown chronologically.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
