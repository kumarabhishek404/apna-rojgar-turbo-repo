appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-home", "availability", "employer", "automated"]
---
# TC-HOME-002: Employer home dashboard loads
# Module: Tab: Home | Screen: Home Tab | Role: Employer
# Type: Availability | Priority: P0
# Route: /(tabs)/index
# Prerequisites: Employer account · Logged in as Employer
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Login as Employer.
# Open Home tab.

# Assertions
# Expected: Home dashboard with employer CTAs visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
