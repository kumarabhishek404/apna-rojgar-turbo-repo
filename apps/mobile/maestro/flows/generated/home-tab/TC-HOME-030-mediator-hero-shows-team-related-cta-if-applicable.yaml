appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-home", "functionality", "mediator", "automated"]
---
# TC-HOME-030: Mediator hero shows team-related CTA if applicable
# Module: Tab: Home | Screen: Home Hero | Role: Mediator
# Type: Functionality | Priority: P2
# Route: HomeHeroSection
# Prerequisites: Mediator account · Mediator with team
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe mediator hero CTAs.

# Assertions
# Expected: Team/work relevant CTAs visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
