appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-home", "ui-design", "worker", "automated"]
---
# TC-HOME-035: Service cards show pay and location info
# Module: Tab: Home | Screen: Featured Services | Role: Worker
# Type: UI Design | Priority: P2
# Route: UnifiedHomeDashboard
# Prerequisites: Worker account · Featured services visible
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe service card content.

# Assertions
# Expected: Daily pay, location or distance visible on cards.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
