appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-home", "availability", "all", "automated"]
---
# TC-HOME-014: Profile completion prompt shown when incomplete
# Module: Tab: Home | Screen: Profile Completion | Role: All
# Type: Availability | Priority: P1
# Route: CompletProfileNotify
# Prerequisites: Any test account · Incomplete profile fields
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Use account with missing fields.
# Open home.

# Assertions
# Expected: Complete profile notification banner visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
