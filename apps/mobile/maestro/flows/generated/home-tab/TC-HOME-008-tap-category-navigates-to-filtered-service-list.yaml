appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-home", "functionality", "all", "automated"]
---
# TC-HOME-008: Tap category navigates to filtered service list
# Module: Tab: Home | Screen: Work Categories | Role: All
# Type: Functionality | Priority: P1
# Route: UnifiedHomeDashboard
# Prerequisites: Any test account · Categories visible
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "a work category"
# Observe destination.

# Assertions
# Expected: Service list filtered by category opens.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
