appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-home", "ui-design", "all", "automated"]
---
# TC-HOME-034: Safe area insets respected on notched devices
# Module: Tab: Home | Screen: Home Tab | Role: All
# Type: UI Design | Priority: P2
# Route: /(tabs)/index
# Prerequisites: Any test account · On home
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Test on notched Android device.
# Observe top/bottom padding.

# Assertions
# Expected: Content not hidden under status bar or nav bar.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
