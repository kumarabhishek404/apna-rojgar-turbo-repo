appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["authentication", "availability", "guest", "automated"]
---
# TC-AUTH-020: Register step 4 shows role selection
# Module: Authentication | Screen: Register Step 4 | Role: Guest
# Type: Availability | Priority: P0
# Route: /screens/auth/register/fourth
# Prerequisites: Logged out · Completed step 2
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open register/fourth.
# Observe options.

# Assertions
# Expected: Worker, Employer, Mediator role cards visible.

# Cleanup
