appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["authentication", "availability", "guest", "automated"]
---
# TC-AUTH-028: Legacy register wizard still accessible
# Module: Authentication | Screen: Legacy Register | Role: Guest
# Type: Availability | Priority: P2
# Route: /screens/auth/register
# Prerequisites: Logged out · Navigate to legacy route
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Navigate to /screens/auth/register.
# Observe wizard.

# Assertions
- assertNotVisible: "Something went wrong"

# Cleanup
