appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["authentication", "functionality", "guest", "automated"]
---
# TC-AUTH-013: Resume registration for existing partial user
# Module: Authentication | Screen: Register Step 1 | Role: Guest
# Type: Functionality | Priority: P1
# Route: /screens/auth/register/first
# Prerequisites: Logged out · Existing partial profile mobile
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn:
    id: "login-mobile-input"
- inputText: ${WORKER_MOBILE}
# Verify OTP.

# Assertions
# Expected: Resumes at correct step (second or fifth).

# Cleanup
