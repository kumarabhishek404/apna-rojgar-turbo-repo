appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "functionality", "employer", "automated"]
---
# TC-ADD-033: Set workers needed and pay per skill
# Module: Post a Job Wizard | Screen: Worker Salary | Role: Employer
# Type: Functionality | Priority: P0
# Route: SelectWorkerSalary.tsx
# Prerequisites: Employer account · On salary step
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Add skill row.
# Set count and daily pay.
# Next.

# Assertions
# Expected: Requirements saved correctly.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
