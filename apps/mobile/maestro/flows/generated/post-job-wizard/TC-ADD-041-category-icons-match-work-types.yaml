appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "ui-design", "employer", "automated"]
---
# TC-ADD-041: Category icons match work types
# Module: Post a Job Wizard | Screen: Select Work Category | Role: Employer
# Type: UI Design | Priority: P2
# Route: SelectWorkCategory.tsx
# Prerequisites: Employer account · On category step
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe category grid.

# Assertions
# Expected: Icons and labels aligned with WORKERTYPES.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
