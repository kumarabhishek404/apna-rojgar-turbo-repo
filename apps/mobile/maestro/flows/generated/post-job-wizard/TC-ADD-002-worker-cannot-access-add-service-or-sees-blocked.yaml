appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "availability", "worker", "automated"]
---
# TC-ADD-002: Worker cannot access add service or sees blocked
# Module: Post a Job Wizard | Screen: Add Service Wizard | Role: Worker
# Type: Availability | Priority: P1
# Route: /screens/addService
# Prerequisites: Worker account · Worker logged in
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Attempt to open add service as Worker.

# Assertions
# Expected: Wizard not accessible or appropriate message.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
