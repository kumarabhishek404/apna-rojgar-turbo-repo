appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "functionality", "employer", "automated"]
---
# TC-ADD-010: Step 3: validation blocks empty required fields
# Module: Post a Job Wizard | Screen: Worker Salary & Requirements | Role: Employer
# Type: Functionality | Priority: P1
# Route: /screens/addService (SelectWorkerSalary.tsx)
# Prerequisites: Employer account · On step 3
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Leave required fields empty.
- tapOn: "Next"

# Assertions
# Expected: Validation error.
# Expected: cannot proceed.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
