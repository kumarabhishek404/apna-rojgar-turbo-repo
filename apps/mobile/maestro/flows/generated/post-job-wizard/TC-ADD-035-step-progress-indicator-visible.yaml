appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "ui-design", "employer", "automated"]
---
# TC-ADD-035: Step progress indicator visible
# Module: Post a Job Wizard | Screen: Add Service Wizard | Role: Employer
# Type: UI Design | Priority: P1
# Route: /screens/addService
# Prerequisites: Employer account · In wizard
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe progress through all steps.

# Assertions
# Expected: User knows current step number.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
