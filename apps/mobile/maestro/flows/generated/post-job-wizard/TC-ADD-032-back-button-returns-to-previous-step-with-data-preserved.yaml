appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "functionality", "employer", "automated"]
---
# TC-ADD-032: Back button returns to previous step with data preserved
# Module: Post a Job Wizard | Screen: Add Service Wizard | Role: Employer
# Type: Functionality | Priority: P1
# Route: /screens/addService
# Prerequisites: Employer account · Mid-wizard
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Fill step 3.
# Go to step 4.
- tapOn: "Back"

# Assertions
# Expected: Step 3 data still filled (Jotai draft).

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
