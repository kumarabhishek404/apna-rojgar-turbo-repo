appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "functionality", "employer", "automated"]
---
# TC-ADD-038: Voice-to-text for description if available
# Module: Post a Job Wizard | Screen: Duration & Description | Role: Employer
# Type: Functionality | Priority: P1
# Route: SetDuration&Description.tsx
# Prerequisites: Employer account · On description step
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "voice input"
# Speak description.

# Assertions
# Expected: Text populated from speech.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
