appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["post-a-job-wizard", "functionality", "employer", "automated"]
---
# TC-ADD-027: Map picker sets job address
# Module: Post a Job Wizard | Screen: Location & Date | Role: Employer
# Type: Functionality | Priority: P0
# Route: SelectLocation&Date.tsx
# Prerequisites: Employer account · On location step
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "map/location"
# Pick point.
# Confirm.

# Assertions
# Expected: Address and coordinates saved.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
