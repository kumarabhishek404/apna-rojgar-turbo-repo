appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-profile", "functionality", "all", "manual-review"]
---
# TC-PROF-016: Change profile photo via camera or gallery
# Module: Tab: Profile | Screen: Profile Photo | Role: All
# Type: Functionality | Priority: P1
# Route: UserProfile
# Prerequisites: Any test account · On profile
# STATUS: MANUAL REVIEW — contains steps not fully automatable
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "edit photo"
# Pick image.
# Save.

# Assertions
# Expected: New photo uploaded and displayed.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
