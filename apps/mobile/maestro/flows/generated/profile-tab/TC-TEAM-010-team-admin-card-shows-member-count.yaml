appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["team-mediator", "ui-design", "mediator", "automated"]
---
# TC-TEAM-010: Team admin card shows member count
# Module: Team / Mediator | Screen: Team Admin Card | Role: Mediator
# Type: UI Design | Priority: P1
# Route: TeamAdminCard
# Prerequisites: Mediator account · Mediator profile
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe team card on profile.

# Assertions
# Expected: Member count and team name visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
