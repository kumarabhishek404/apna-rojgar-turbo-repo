appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-profile", "availability", "all", "automated"]
---
# TC-PROF-008: Role switcher visible on profile
# Module: Tab: Profile | Screen: Role Switcher | Role: All
# Type: Availability | Priority: P1
# Route: RoleSwitcher
# Prerequisites: Any test account · Multi-role account or switcher enabled
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Find RoleSwitcher on profile.

# Assertions
# Expected: Worker/Employer/Mediator options shown.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
