appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-profile", "availability", "all", "automated"]
---
# TC-PROF-003: Overview sub-tab shows stats and info
# Module: Tab: Profile | Screen: Profile Overview | Role: All
# Type: Availability | Priority: P1
# Route: UserProfile
# Prerequisites: Any test account · On profile tab
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Select Overview tab.

# Assertions
# Expected: Work info, likes, skills sections visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
