appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["admin-panel", "availability", "admin", "automated"]
---
# TC-ADM-013: Admin profile tab loads
# Module: Admin Panel | Screen: Admin Profile | Role: Admin
# Type: Availability | Priority: P1
# Route: AdminProfile
# Prerequisites: Admin account · Admin on profile tab
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open profile as Admin.

# Assertions
# Expected: AdminProfile with admin settings.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
