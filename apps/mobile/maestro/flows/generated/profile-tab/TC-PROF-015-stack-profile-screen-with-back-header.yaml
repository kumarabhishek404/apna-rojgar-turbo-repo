appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-profile", "availability", "all", "automated"]
---
# TC-PROF-015: Stack profile screen with back header
# Module: Tab: Profile | Screen: Full Profile Screen | Role: All
# Type: Availability | Priority: P2
# Route: /screens/profile
# Prerequisites: Any test account · Navigate from menu
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open /screens/profile from link.

# Assertions
# Expected: Full-screen profile with back button.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
