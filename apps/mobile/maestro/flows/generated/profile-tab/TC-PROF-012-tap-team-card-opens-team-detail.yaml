appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-profile", "functionality", "mediator", "automated"]
---
# TC-PROF-012: Tap team card opens team detail
# Module: Tab: Profile | Screen: Team Admin Card | Role: Mediator
# Type: Functionality | Priority: P1
# Route: TeamAdminCard
# Prerequisites: Mediator account · Team card visible
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "team admin card"

# Assertions
# Expected: Navigates to team members screen.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
