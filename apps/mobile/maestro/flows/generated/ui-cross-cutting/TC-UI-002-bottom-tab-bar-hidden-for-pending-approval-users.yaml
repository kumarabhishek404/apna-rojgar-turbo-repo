appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["cross-cutting-ui-design", "ui-design", "all", "automated"]
---
# TC-UI-002: Bottom tab bar hidden for pending approval users
# Module: Cross-cutting UI Design | Screen: Tab Bar | Role: All
# Type: UI Design | Priority: P0
# Route: /(tabs)
# Prerequisites: Any test account · Pending approval account
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Login pending account.
# Observe layout.

# Assertions
- assertVisible:
    id: "tab-home"
- assertVisible:
    id: "tab-profile"
# Expected: profile full screen.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
