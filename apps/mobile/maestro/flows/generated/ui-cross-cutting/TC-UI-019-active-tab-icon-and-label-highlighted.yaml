appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["cross-cutting-ui-design", "ui-design", "all", "manual-review"]
---
# TC-UI-019: Active tab icon and label highlighted
# Module: Cross-cutting UI Design | Screen: Tab Active State | Role: All
# Type: UI Design | Priority: P1
# Route: /(tabs)
# Prerequisites: Any test account · On any tab
# STATUS: MANUAL REVIEW — contains steps not fully automatable
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Switch between all 5 tabs.

# Assertions
# Expected: Active tab visually distinct each time.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
