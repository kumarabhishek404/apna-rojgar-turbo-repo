appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["cross-cutting-ui-design", "ui-design", "all", "automated"]
---
# TC-UI-003: Confirmation modals have clear title message and buttons
# Module: Cross-cutting UI Design | Screen: Modals | Role: All
# Type: UI Design | Priority: P1
# Route: ModalComponent
# Prerequisites: Any test account · Trigger delete/complete/deactivate
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Trigger delete service, complete booking, deactivate account modals.

# Assertions
# Expected: Each modal has title, message, Confirm/Cancel.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
