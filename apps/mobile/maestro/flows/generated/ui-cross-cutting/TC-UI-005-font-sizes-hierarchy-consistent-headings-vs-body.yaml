appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["cross-cutting-ui-design", "ui-design", "all", "manual-review"]
---
# TC-UI-005: Font sizes hierarchy consistent (headings vs body)
# Module: Cross-cutting UI Design | Screen: Typography | Role: All
# Type: UI Design | Priority: P2
# Route: Global
# Prerequisites: Any test account · Multiple screens
# STATUS: MANUAL REVIEW — contains steps not fully automatable
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Compare heading and body text sizes.

# Assertions
# Expected: Clear visual hierarchy.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
