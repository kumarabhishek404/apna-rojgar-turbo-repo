appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["cross-cutting-ui-design", "ui-design", "all", "manual-review"]
---
# TC-UI-024: Touchable list items show press feedback
# Module: Cross-cutting UI Design | Screen: List Item Ripple | Role: All
# Type: UI Design | Priority: P2
# Route: Android
# Prerequisites: Any test account · Android device
# STATUS: MANUAL REVIEW — contains steps not fully automatable
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "list items"
# Observe ripple/highlight.

# Assertions
# Expected: Visual press feedback on tap.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
