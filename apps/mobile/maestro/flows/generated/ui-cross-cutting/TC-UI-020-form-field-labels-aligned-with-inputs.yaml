appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["cross-cutting-ui-design", "ui-design", "guest", "automated"]
---
# TC-UI-020: Form field labels aligned with inputs
# Module: Cross-cutting UI Design | Screen: Form Labels | Role: Guest
# Type: UI Design | Priority: P1
# Route: Auth/Register
# Prerequisites: Logged out · Registration forms
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Review register step 2 form layout.

# Assertions
# Expected: Labels above or beside inputs consistently.

# Cleanup
