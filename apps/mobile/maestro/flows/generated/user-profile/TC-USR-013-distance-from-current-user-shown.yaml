appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "ui-design", "all", "automated"]
---
# TC-USR-013: Distance from current user shown
# Module: User Browse & Profile Actions | Screen: User Profile | Role: All
# Type: UI Design | Priority: P1
# Route: /screens/users/[id]
# Prerequisites: Any test account · User with location
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe distance label.

# Assertions
# Expected: Distance in km shown if location available.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
