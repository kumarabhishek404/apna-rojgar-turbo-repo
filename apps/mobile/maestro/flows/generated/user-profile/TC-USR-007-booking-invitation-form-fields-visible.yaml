appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "availability", "employer", "automated"]
---
# TC-USR-007: Booking invitation form fields visible
# Module: User Browse & Profile Actions | Screen: Add Booking Details | Role: Employer
# Type: Availability | Priority: P0
# Route: /screens/users/addBookingDetails
# Prerequisites: Employer account · From user profile book action
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open add booking details.

# Assertions
# Expected: Date, service, message fields visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
