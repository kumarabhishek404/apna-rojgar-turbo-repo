appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "functionality", "all", "automated"]
---
# TC-USR-002: Users list filters by saved/booked params
# Module: User Browse & Profile Actions | Screen: Users List | Role: All
# Type: Functionality | Priority: P1
# Route: /screens/users
# Prerequisites: Any test account · List with filters
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open with filter params.

# Assertions
# Expected: List filtered accordingly.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
