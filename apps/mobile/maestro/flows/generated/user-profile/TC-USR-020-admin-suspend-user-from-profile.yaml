appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "functionality", "admin", "automated"]
---
# TC-USR-020: Admin suspend user from profile
# Module: User Browse & Profile Actions | Screen: User Profile | Role: Admin
# Type: Functionality | Priority: P1
# Route: /screens/users/[id]
# Prerequisites: Admin account · Admin viewing user
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "Suspend if available"

# Assertions
# Expected: User suspended.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
