appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "availability", "mediator", "automated"]
---
# TC-USR-021: Mediator team info on user info box
# Module: User Browse & Profile Actions | Screen: User Info Box | Role: Mediator
# Type: Availability | Priority: P2
# Route: UserInfoBox
# Prerequisites: Mediator account · Mediator profile
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# View mediator profile.

# Assertions
# Expected: Team size info visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
