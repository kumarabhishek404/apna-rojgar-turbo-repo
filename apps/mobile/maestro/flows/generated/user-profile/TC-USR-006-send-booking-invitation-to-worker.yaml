appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "functionality", "employer", "automated"]
---
# TC-USR-006: Send booking invitation to worker
# Module: User Browse & Profile Actions | Screen: User Profile | Role: Employer
# Type: Functionality | Priority: P0
# Route: /screens/users/[id]
# Prerequisites: Employer account · Employer viewing worker
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "Book/Send Invitation"
# Fill booking details.
# Send.

# Assertions
# Expected: Invitation sent.
# Expected: success toast.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
