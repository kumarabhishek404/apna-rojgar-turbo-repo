appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "ui-design", "all", "automated"]
---
# TC-USR-028: Skills list wraps without overflow
# Module: User Browse & Profile Actions | Screen: User Profile | Role: All
# Type: UI Design | Priority: P2
# Route: /screens/users/[id]
# Prerequisites: Any test account · User with many skills
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# View user with 10+ skills.

# Assertions
# Expected: Skills displayed cleanly.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
