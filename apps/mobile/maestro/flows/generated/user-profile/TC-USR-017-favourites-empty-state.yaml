appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "ui-design", "all", "automated"]
---
# TC-USR-017: Favourites empty state
# Module: User Browse & Profile Actions | Screen: Favourites | Role: All
# Type: UI Design | Priority: P2
# Route: /screens/favourite
# Prerequisites: Any test account · No favourites
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open favourites with none saved.

# Assertions
# Expected: Empty state message.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
