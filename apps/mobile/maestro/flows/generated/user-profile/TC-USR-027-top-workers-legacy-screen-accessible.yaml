appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["user-browse-profile-actions", "availability", "employer", "automated"]
---
# TC-USR-027: Top workers legacy screen accessible
# Module: User Browse & Profile Actions | Screen: Users List | Role: Employer
# Type: Availability | Priority: P2
# Route: /screens/bottomTabs/(user)/employer/topWorkers
# Prerequisites: Employer account · Navigate to top workers
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open topWorkers if linked.

# Assertions
# Expected: Top workers list loads.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
