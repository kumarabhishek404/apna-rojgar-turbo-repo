appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["authentication", "availability", "guest", "automated"]
---
# TC-AUTH-032: Legacy PIN step accessible but skipped in active flow
# Module: Authentication | Screen: Register Step 3 (Legacy PIN) | Role: Guest
# Type: Availability | Priority: P2
# Route: /screens/auth/register/third
# Prerequisites: Logged out · Navigate to step 3 directly
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open register/third.
# Observe PIN setup.

# Assertions
# Expected: 4-digit PIN UI loads.
# Expected: active flow skips this step.

# Cleanup
