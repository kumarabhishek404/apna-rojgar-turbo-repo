appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["accessibility-inputs", "functionality", "guest", "automated"]
---
# TC-A11Y-007: Mobile input accepts digits only
# Module: Accessibility & Inputs | Screen: Mobile Number Input | Role: Guest
# Type: Functionality | Priority: P1
# Route: MobileNumber.tsx
# Prerequisites: Logged out · On login
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Try entering letters.

# Assertions
# Expected: Non-numeric rejected or filtered.

# Cleanup
