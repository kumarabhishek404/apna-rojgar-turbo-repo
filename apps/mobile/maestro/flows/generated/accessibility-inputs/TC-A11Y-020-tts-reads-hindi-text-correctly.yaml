appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["accessibility-inputs", "ui-design", "all", "automated"]
---
# TC-A11Y-020: TTS reads Hindi text correctly
# Module: Accessibility & Inputs | Screen: Text to Speech | Role: All
# Type: UI Design | Priority: P2
# Route: TextToSpeech
# Prerequisites: Any test account · Hindi content
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Set Hindi.
# Open service with Hindi description.
# Play TTS.

# Assertions
# Expected: Hindi text spoken intelligibly.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
