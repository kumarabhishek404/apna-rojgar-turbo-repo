appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["accessibility-inputs", "functionality", "all", "automated"]
---
# TC-A11Y-015: Picked image URI normalized for upload
# Module: Accessibility & Inputs | Screen: Image Upload | Role: All
# Type: Functionality | Priority: P1
# Route: normalizePickedImageUriForUpload.ts
# Prerequisites: Any test account · Image pick flow
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Pick image from gallery.
# Upload.

# Assertions
# Expected: Upload succeeds without URI errors on Android.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
