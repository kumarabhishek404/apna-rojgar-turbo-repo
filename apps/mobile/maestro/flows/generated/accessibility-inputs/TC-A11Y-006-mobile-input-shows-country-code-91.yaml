appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["accessibility-inputs", "ui-design", "guest", "automated"]
---
# TC-A11Y-006: Mobile input shows country code +91
# Module: Accessibility & Inputs | Screen: Mobile Number Input | Role: Guest
# Type: UI Design | Priority: P1
# Route: MobileNumber.tsx
# Prerequisites: Logged out · Login or register
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe mobile field on login.

# Assertions
# Expected: India country code displayed.
# Expected: 10-digit input.

# Cleanup
