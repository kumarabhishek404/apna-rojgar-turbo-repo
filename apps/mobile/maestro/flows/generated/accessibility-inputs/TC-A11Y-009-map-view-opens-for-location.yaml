appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["accessibility-inputs", "availability", "all", "automated"]
---
# TC-A11Y-009: Map view opens for location
# Module: Accessibility & Inputs | Screen: View Map | Role: All
# Type: Availability | Priority: P1
# Route: ViewMap.tsx
# Prerequisites: Any test account · Job or user with location
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "view map on service detail"

# Assertions
# Expected: Map with marker at location.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
