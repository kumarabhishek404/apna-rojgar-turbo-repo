appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["accessibility-inputs", "ui-design", "guest", "automated"]
---
# TC-A11Y-018: Role selection cards tappable with clear selection
# Module: Accessibility & Inputs | Screen: Select Role Input | Role: Guest
# Type: UI Design | Priority: P1
# Route: SelectRole.tsx
# Prerequisites: Logged out · Register step 4
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "each role card"

# Assertions
# Expected: Selected card highlighted.

# Cleanup
