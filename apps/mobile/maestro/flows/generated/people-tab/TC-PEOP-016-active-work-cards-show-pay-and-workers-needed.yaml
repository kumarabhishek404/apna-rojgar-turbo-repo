appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-people", "ui-design", "mediator", "automated"]
---
# TC-PEOP-016: Active work cards show pay and workers needed
# Module: Tab: People | Screen: Active Works List | Role: Mediator
# Type: UI Design | Priority: P1
# Route: UnifiedPeopleScreen
# Prerequisites: Mediator account · Jobs listed
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe job cards.

# Assertions
# Expected: Pay rate and worker count visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
