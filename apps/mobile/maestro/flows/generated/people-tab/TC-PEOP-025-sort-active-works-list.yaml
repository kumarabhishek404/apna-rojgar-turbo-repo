appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-people", "functionality", "mediator", "automated"]
---
# TC-PEOP-025: Sort active works list
# Module: Tab: People | Screen: People Tab | Role: Mediator
# Type: Functionality | Priority: P2
# Route: /(tabs)/third
# Prerequisites: Mediator account · Sort on active works
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Apply sort tab if available.

# Assertions
# Expected: Jobs re-ordered.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
