appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-people", "availability", "all", "automated"]
---
# TC-PEOP-023: People tab handles offline gracefully
# Module: Tab: People | Screen: People Tab | Role: All
# Type: Availability | Priority: P1
# Route: /(tabs)/third
# Prerequisites: Any test account · Offline
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Disable network.
# Open people tab.

# Assertions
# Expected: Offline message or cached data.
- assertNotVisible: "Something went wrong"

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
