appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-people", "ui-design", "worker", "automated"]
---
# TC-PEOP-024: Contractor profile photos display correctly
# Module: Tab: People | Screen: Contractor List | Role: Worker
# Type: UI Design | Priority: P2
# Route: UnifiedPeopleScreen
# Prerequisites: Worker account · Contractors with photos
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe avatar images.

# Assertions
# Expected: Photos load or placeholder shown.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
