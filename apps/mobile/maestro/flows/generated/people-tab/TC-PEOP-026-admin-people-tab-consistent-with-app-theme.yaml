appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-people", "ui-design", "admin", "automated"]
---
# TC-PEOP-026: Admin people tab consistent with app theme
# Module: Tab: People | Screen: People Tab | Role: Admin
# Type: UI Design | Priority: P2
# Route: /(tabs)/third
# Prerequisites: Admin account · Admin on people tab
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-admin.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Observe layout and colors.

# Assertions
# Expected: Consistent with admin theme.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
