appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["language-onboarding", "functionality", "guest", "automated"]
---
# TC-ONB-002: Selecting language applies locale and navigates forward
# Module: Language & Onboarding | Screen: Language Selection | Role: Guest
# Type: Functionality | Priority: P0
# Route: /languageSelection
# Prerequisites: Logged out · On language selection screen
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "a language"
# Observe next screen.

# Assertions
# Expected: App strings switch to selected language.
# Expected: navigates to onboarding or login path.

# Cleanup
