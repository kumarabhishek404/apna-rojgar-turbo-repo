appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["language-onboarding", "ui-design", "guest", "automated"]
---
# TC-ONB-003: Language selection UI is readable and tappable on small Android screens
# Module: Language & Onboarding | Screen: Language Selection | Role: Guest
# Type: UI Design | Priority: P1
# Route: /languageSelection
# Prerequisites: Logged out · On language selection screen
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Test on small screen device.
# Scroll if needed.
- tapOn: "each language row"

# Assertions
# Expected: No text truncation.
# Expected: touch targets adequate.
# Expected: no overlap.

# Cleanup
