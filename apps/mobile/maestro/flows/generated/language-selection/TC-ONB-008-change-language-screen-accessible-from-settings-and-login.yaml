appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["language-onboarding", "availability", "all", "automated"]
---
# TC-ONB-008: Change language screen accessible from settings and login
# Module: Language & Onboarding | Screen: Change Language | Role: All
# Type: Availability | Priority: P1
# Route: /screens/settings/changeLanguage
# Prerequisites: Any test account · Logged in; open from login or profile settings
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Profile → Settings → Change Language.
# Also test link from login screen.

# Assertions
# Expected: Language list screen opens from both entry points.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
