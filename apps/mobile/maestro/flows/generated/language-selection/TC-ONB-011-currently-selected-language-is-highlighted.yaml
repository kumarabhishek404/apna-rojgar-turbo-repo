appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["language-onboarding", "ui-design", "all", "automated"]
---
# TC-ONB-011: Currently selected language is highlighted
# Module: Language & Onboarding | Screen: Change Language | Role: All
# Type: UI Design | Priority: P2
# Route: /screens/settings/changeLanguage
# Prerequisites: Any test account · On change language screen
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open change language.
# Observe selected language marker.

# Assertions
# Expected: Active language has checkmark or highlight.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
