appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["language-onboarding", "functionality", "guest", "automated"]
---
# TC-ONB-015: Bengali locale selection works
# Module: Language & Onboarding | Screen: Language Selection | Role: Guest
# Type: Functionality | Priority: P1
# Route: /languageSelection
# Prerequisites: Logged out · Regional language list
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Select Bengali (bn).
# Verify strings on next screen.

# Assertions
# Expected: Bengali translations applied.

# Cleanup
