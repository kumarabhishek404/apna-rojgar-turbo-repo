appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["language-onboarding", "functionality", "guest", "automated"]
---
# TC-ONB-018: Skip onboarding if skip button present
# Module: Language & Onboarding | Screen: User Tips / Onboarding | Role: Guest
# Type: Functionality | Priority: P2
# Route: /screens/userTipForAppUse
# Prerequisites: Logged out · On onboarding
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Look for Skip button.
- tapOn: "if available"

# Assertions
# Expected: User proceeds without viewing all slides.

# Cleanup
