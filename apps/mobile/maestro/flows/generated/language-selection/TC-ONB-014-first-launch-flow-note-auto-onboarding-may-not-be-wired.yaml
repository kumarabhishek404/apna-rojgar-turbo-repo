appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["language-onboarding", "functionality", "guest", "automated"]
---
# TC-ONB-014: First launch flow note — auto onboarding may not be wired
# Module: Language & Onboarding | Screen: First Launch | Role: Guest
# Type: Functionality | Priority: P2
# Route: useFirstTimeLaunch.tsx
# Prerequisites: Logged out · Fresh install
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/ensure-logged-out.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Fresh install.
# Observe if language/onboarding auto-shows.

# Assertions
# Expected: Document actual behavior.
# Expected: manual navigation to languageSelection works.

# Cleanup
