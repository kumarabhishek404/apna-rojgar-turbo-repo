appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
tags: ["global-app-shell", "functionality", "all", "automated"]
---
# TC-GLB-024: Suspended account shows lock screen with support email
# Module: Global & App Shell | Screen: Suspended Account | Role: All
# Type: Functionality | Priority: P0
# Route: components/commons/AccountSuspendedScreen.tsx
# Prerequisites: Any test account · Account suspended on backend
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Login or use suspended account.
# Observe the app.

# Assertions
- assertVisible:
    id: account-suspended-screen
# Expected: Only the suspended-account screen is shown.
# Expected: Policy violation message and support email are visible.
# Expected: Other tabs/screens are not usable.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
