appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "ui-design", "worker", "automated"]
---
# TC-ACT-013: Worker Work Requests empty state
# Module: Tab: Activity | Screen: Activity - Work Requests | Role: Worker
# Type: UI Design | Priority: P2
# Route: UnifiedActivityScreen
# Prerequisites: Worker account · Worker on Work Requests empty
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Work Requests with no data.

# Assertions
# Expected: Empty state message shown.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
