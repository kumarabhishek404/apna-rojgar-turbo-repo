appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "availability", "employer", "automated"]
---
# TC-ACT-002: Employer activity tab loads with sub-tabs
# Module: Tab: Activity | Screen: Activity Tab | Role: Employer
# Type: Availability | Priority: P0
# Route: /(tabs)/fourth
# Prerequisites: Employer account · Employer logged in
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Activity tab as Employer.

# Assertions
# Expected: Employer-specific activity sub-tabs visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
