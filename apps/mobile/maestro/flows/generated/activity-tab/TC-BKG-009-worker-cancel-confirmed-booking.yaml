appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["bookings-invitations", "functionality", "worker", "automated"]
---
# TC-BKG-009: Worker cancel confirmed booking
# Module: Bookings & Invitations | Screen: Worker Bookings | Role: Worker
# Type: Functionality | Priority: P0
# Route: workerBookings.tsx
# Prerequisites: Worker account · Confirmed booking
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open booking.
# Cancel if allowed.

# Assertions
# Expected: Booking cancelled per rules.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
