appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "functionality", "mediator", "automated"]
---
# TC-ACT-021: Mediator Team Bookings list item tap navigates
# Module: Tab: Activity | Screen: Activity - Team Bookings | Role: Mediator
# Type: Functionality | Priority: P1
# Route: UnifiedActivityScreen
# Prerequisites: Mediator account · Mediator Team Bookings with data
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "item in Team Bookings"

# Assertions
# Expected: Correct detail screen opens.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
