appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "functionality", "worker", "automated"]
---
# TC-ACT-012: Worker Work Requests list shows items and navigates on tap
# Module: Tab: Activity | Screen: Activity - Work Requests | Role: Worker
# Type: Functionality | Priority: P1
# Route: UnifiedActivityScreen
# Prerequisites: Worker account · Worker on Work Requests tab with data
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Work Requests sub-tab.
- tapOn: "a list item"

# Assertions
# Expected: Navigates to relevant detail screen.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
