appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "functionality", "employer", "automated"]
---
# TC-ACT-031: Employer cancel sent invitation
# Module: Tab: Activity | Screen: Employer Work Requests | Role: Employer
# Type: Functionality | Priority: P1
# Route: employerWorkRequests.tsx
# Prerequisites: Employer account · Sent invitation
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-employer.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Sent Invitations.
# Cancel invitation.

# Assertions
# Expected: Invitation cancelled.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
