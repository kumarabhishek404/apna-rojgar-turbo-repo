appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "availability", "all", "automated"]
---
# TC-ACT-039: Legacy bookingsAndRequests screen accessible
# Module: Tab: Activity | Screen: Legacy Bookings Requests | Role: All
# Type: Availability | Priority: P2
# Route: /screens/bottomTabs/(user)/bookingsAndRequests
# Prerequisites: Any test account · Navigate to legacy route
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Navigate to legacy route if linked.

# Assertions
# Expected: Bookings/Requests switcher loads.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
