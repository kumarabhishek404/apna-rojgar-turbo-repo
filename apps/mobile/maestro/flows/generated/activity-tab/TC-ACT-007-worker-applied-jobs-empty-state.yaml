appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "ui-design", "worker", "automated"]
---
# TC-ACT-007: Worker Applied Jobs empty state
# Module: Tab: Activity | Screen: Activity - Applied Jobs | Role: Worker
# Type: UI Design | Priority: P2
# Route: UnifiedActivityScreen
# Prerequisites: Worker account · Worker on Applied Jobs empty
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Applied Jobs with no data.

# Assertions
# Expected: Empty state message shown.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
