appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "availability", "mediator", "automated"]
---
# TC-ACT-024: Mediator Applied Services sub-tab visible
# Module: Tab: Activity | Screen: Activity - Applied Services | Role: Mediator
# Type: Availability | Priority: P1
# Route: UnifiedActivityScreen
# Prerequisites: Mediator account · Mediator on activity tab
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "Applied Services sub-tab"

# Assertions
# Expected: Applied Services section loads.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
