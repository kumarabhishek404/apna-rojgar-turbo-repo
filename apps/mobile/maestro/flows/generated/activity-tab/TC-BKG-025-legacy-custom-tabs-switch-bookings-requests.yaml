appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["bookings-invitations", "availability", "all", "automated"]
---
# TC-BKG-025: Legacy custom tabs switch bookings/requests
# Module: Bookings & Invitations | Screen: Custom Tabs | Role: All
# Type: Availability | Priority: P2
# Route: bookingsAndRequests/customTabs
# Prerequisites: Any test account · Legacy bookings container
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open legacy container.
# Switch tabs.

# Assertions
# Expected: Content switches between bookings and requests.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
