appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "availability", "mediator", "automated"]
---
# TC-ACT-003: Mediator activity tab loads with sub-tabs
# Module: Tab: Activity | Screen: Activity Tab | Role: Mediator
# Type: Availability | Priority: P0
# Route: /(tabs)/fourth
# Prerequisites: Mediator account · Mediator logged in
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-mediator.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Activity tab as Mediator.

# Assertions
# Expected: Mediator-specific sub-tabs visible.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
