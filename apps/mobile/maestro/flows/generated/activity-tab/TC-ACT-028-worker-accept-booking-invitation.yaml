appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-activity", "functionality", "worker", "automated"]
---
# TC-ACT-028: Worker accept booking invitation
# Module: Tab: Activity | Screen: Worker Work Requests | Role: Worker
# Type: Functionality | Priority: P0
# Route: workerWorkRequests.tsx
# Prerequisites: Worker account · Pending invitation
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Open Work Requests.
- tapOn: "Accept on invitation"

# Assertions
# Expected: Invitation accepted.
# Expected: moves to bookings.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
