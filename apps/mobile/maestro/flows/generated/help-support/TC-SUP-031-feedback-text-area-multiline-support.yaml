appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["settings-support-legal", "ui-design", "all", "automated"]
---
# TC-SUP-031: Feedback text area multiline support
# Module: Settings, Support & Legal | Screen: App Feedback | Role: All
# Type: UI Design | Priority: P2
# Route: /screens/appFeedback
# Prerequisites: Any test account · Long feedback text
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Enter long feedback.

# Assertions
# Expected: Text wraps without layout break.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
