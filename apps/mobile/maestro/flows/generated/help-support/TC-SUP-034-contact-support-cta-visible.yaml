appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["settings-support-legal", "availability", "all", "automated"]
---
# TC-SUP-034: Contact support CTA visible
# Module: Settings, Support & Legal | Screen: Contact Support | Role: All
# Type: Availability | Priority: P2
# Route: ContactSupport
# Prerequisites: Any test account · On profile or help
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
# Find Contact Support button.

# Assertions
# Expected: CTA visible and tappable.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
