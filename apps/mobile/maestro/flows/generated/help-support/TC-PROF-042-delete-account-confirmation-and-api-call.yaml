appId: com.kumarabhishek404.labourapp
env:
  STAGING_OTP: ${STAGING_OTP}
  WORKER_MOBILE: ${WORKER_MOBILE}
  EMPLOYER_MOBILE: ${EMPLOYER_MOBILE}
  MEDIATOR_MOBILE: ${MEDIATOR_MOBILE}
  ADMIN_MOBILE: ${ADMIN_MOBILE}
tags: ["tab-profile", "functionality", "all", "automated"]
---
# TC-PROF-042: Delete account confirmation and API call
# Module: Tab: Profile | Screen: Delete Account | Role: All
# Type: Functionality | Priority: P0
# Route: /screens/profile/deleteProfile
# Prerequisites: Any test account · On delete profile screen
# Preconditions
- launchApp:
    clearState: false
- runFlow: ../../helpers/login-worker.yaml

# Test steps (from manual case — adjust selectors if UI changes)
- tapOn: "Delete Account"
# Confirm in modal.

# Assertions
# Expected: Account deletion initiated.
# Expected: logged out.

# Cleanup
# Optional: - runFlow: ../../helpers/logout.yaml
