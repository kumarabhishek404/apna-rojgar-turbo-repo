/** DD/MM/YYYY for Google Sheets exports (Asia/Kolkata). */
export const formatSheetDate = (date) => {
  if (!date) return "";
  return new Date(date).toLocaleDateString("en-GB", {
    day: "2-digit",
    month: "2-digit",
    year: "numeric",
    timeZone: "Asia/Kolkata",
  });
};
